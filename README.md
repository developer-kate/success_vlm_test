python package
```
pip install opencv-python
pip install numpy
pip install pandas
pip install python-dotenv
pip install mediapipe
pip install torch torchvision
pip install requests
```

Gemini API
```
pip install google-generativeai
```

Directory Structure
```project/
├── video/
│   └── r1test.mp4          # 분석할 비디오 파일
├── results/                 # 자동 생성됨
├── logs/                    # 자동 생성됨
├── temp/                    # 자동 생성됨
├── robot_scripts/          # 자동 생성됨
├── accuracy_results/       # 자동 생성됨
├── main_vlm.py             # 첫 번째 스크립트
├── pytorch_analysis.py     # 두 번째 스크립트
└── .env                    # API 키 저장
```

